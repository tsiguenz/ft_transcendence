export const API_URL = `http://${window.location.hostname}:3000/api`;
export const FRONT_URL = `http://${window.location.hostname}:8080`;
export const CHAT_SOCKET_URL = `http://${window.location.hostname}:3000/chat`;
